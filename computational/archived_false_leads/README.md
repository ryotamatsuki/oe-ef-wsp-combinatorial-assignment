# Archived false lead: six-type candidate-face INFEASIBLE

**Status: INVALIDATED**

Historical claim: `D†={A,B,E,F,C,D}`, `delta=1/1000` was reported INFEASIBLE by an ephemeral candidate-face model.

Reason for invalidation: an explicit analytic OE+EF+full-SD-SP mechanism exists on exactly the same domain. Cross-group deviations have truthful SD margin `1/3`, so the witness also satisfies the historical fixed margin.

**Do not cite the old INFEASIBLE result as evidence of impossibility.**

Root cause: **unresolved at line level because the legacy source/model matrix was not preserved**. The leading hypothesis is incomplete/misencoded candidate-face handling, particularly legal OE points with `ab=0` for pair-first C/D types, but this is not asserted as proven.
